# Server Configuration
PORT=3001
NODE_ENV=production

# Database Configuration (Configure com os dados do EasyPanel)
DB_HOST=seu-postgres-host.easypanel.host
DB_PORT=5432
DB_NAME=crm_database
DB_USER=seu_usuario_postgres
DB_PASSWORD=sua_senha_postgres

# JWT Configuration (MUDE para uma chave aleatória forte!)
JWT_SECRET=MUDE_ESTA_CHAVE_SECRETA_AQUI_USE_OPENSSL_RAND_BASE64_32
JWT_EXPIRES_IN=7d

# CORS Configuration (URL do seu frontend)
CORS_ORIGIN=https://seu-dominio.com